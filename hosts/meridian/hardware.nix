# Hardware configuration for omen
# TODO: Add your HP laptop hardware configuration here
# See /home/jacob/src/nixConfig/hosts/thonkpad/hardware.nix for reference
{ config, lib, pkgs, ... }:

{
  imports = [];

  # You'll need to generate this with:
  # nixos-generate-config --show-hardware-config
  # and paste the output below
}
